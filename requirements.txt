# BrainIAK tutorials — Python dependencies
# Tested with Python 3.11 and 3.13 on macOS. See README.md for full setup.
#
# Note: mpi4py needs an MPI implementation on your system BEFORE `pip install`:
#   macOS:  brew install open-mpi
#   Ubuntu: sudo apt install -y libopenmpi-dev
#   Windows: use conda instead (`conda install -c conda-forge mpi4py`)

# --- Core scientific stack ---------------------------------------------------
numpy>=1.26
scipy>=1.11
pandas>=2.0
matplotlib>=3.8
seaborn>=0.13
scikit-learn>=1.3
statsmodels>=0.14
networkx>=3.2

# --- Neuroimaging ------------------------------------------------------------
nibabel>=5.2
nilearn>=0.10
deepdish>=0.3.7
pydicom>=2.4

# --- BrainIAK + friends ------------------------------------------------------
# Installed from GitHub to match the version used to build these tutorials.
brainiak @ git+https://github.com/brainiak/brainiak@66e71dd25efdf61f331bbdc3b9f6831c65617127
mpi4py>=3.1
pymanopt>=2.2
nitime>=0.10

# --- Jupyter + interactive widgets -------------------------------------------
jupyterlab>=4.0
notebook>=7.0
ipywidgets>=8.0
ipyvolume>=0.6
niwidgets>=0.2.1
nxviz>=0.6

# --- Utilities ---------------------------------------------------------------
gdown>=5.0
tqdm>=4.66
watchdog>=4.0
